slave:
  build: slave
  links:
    - master:master
    - repo:repo

master:
  build: jenkins
  ports:
    - "0.0.0.0:8080:8080"

repo:
  build: repo
  links:
    - master:master
  ports:
  - "0.0.0.0:8081:80"
