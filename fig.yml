slave:
  build: slave
  links:
    - master:master
    - repo:repo
  privileged: true

master:
  build: jenkins
  ports:
    - "0.0.0.0:8080:8080"
  privileged: true

repo:
  build: repo
  links:
    - master:master
  ports:
  - "0.0.0.0:8081:80"
